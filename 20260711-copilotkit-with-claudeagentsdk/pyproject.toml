[project]
name = "20260711-copilotkit-with-claudeagentsdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "claude-agent-sdk>=0.1.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
markers = [
    "vertex: calls Claude through Google Vertex AI (requires credentials and incurs cost)",
]
pythonpath = ["."]
testpaths = ["tests"]
