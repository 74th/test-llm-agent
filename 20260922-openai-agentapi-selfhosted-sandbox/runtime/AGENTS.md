# Self-hosted executor instructions

When this workspace is active, follow the configuration probe skill when the
user asks to verify agent configuration. The probe token is `AGENTS_PROBE=jade-lantern`.
