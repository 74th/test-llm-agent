---
name: do
---
langchain-deepagent で作ったagentが server/server.py にあるよ。
このコードのcreate_agent関数でagentを作っているよ。

これを FastAPI で WebSocket サーバーとして動かすようにして。

そして、WebSocket で接続して対話できるWebUIをReactで作って。
実装は TypeScript で行うようにして。
