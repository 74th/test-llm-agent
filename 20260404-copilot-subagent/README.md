# subagentのテスト

subagentは以下のチャットで作成した

```
/create-agent Pythonコードのユニットテストを実行するエージェントを作成して
作成したエージェントを、ユニットテスト実行時に使うように、AGENTS.mdに記述して
```
[.github/agents/python-unit-test-runner.agent.md](.github/agents/python-unit-test-runner.agent.md)

実際に `/do` で実行させると、実行させているのがデバッグログから分かる。
