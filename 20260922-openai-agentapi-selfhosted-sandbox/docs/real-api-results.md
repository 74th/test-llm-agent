# 実 API 通し検証記録

最終更新: 2026-09-22

## 状態

完了。2026-09-22 に `gpt-5.6-luna` で実 API 検証を実行し、すべてのシナリオが合格した。詳細な JSON レポートはローカルの `reports/real-api-report.json` に保存される。

```bash
self-hosted-verify verify --output reports/real-api-report.json
```

## 記録欄

| 項目 | 実測結果 |
| --- | --- |
| モデル | `gpt-5.6-luna` |
| マーカー読み取りとツール結果照合 | 合格。`sess_0dcf301460c3b4ac006ab228084d7881a393260f4f2657bf8b` の環境とコンテナマーカーが回答・ツール結果で一致 |
| 2 セッション並行分離 | 合格。`sess_0dcea146bb2ed7c2006ab228180e0081a0a7533042d18df435` と `sess_0af9c6929205dde2006ab228180dec819e840008ae309a85d3` が別環境・別コンテナに対応 |
| コンテナ停止後の同一セッション再開 | 合格。`sess_056644bdd170b264006ab2282be13481a18b174ba502e1c668` で会話が継続 |
| セッション ID と環境 ID の対応 | 合格。各シナリオの JSON レポートと lifecycle JSONL に記録 |
| ファイル状態の非継続 | 合格。永続マウントなしの再起動後コンテナにテストファイルは存在しない |
| ライフサイクル JSONL | 合格。起動、接続、切断、停止、起動失敗を UTC 時刻と ID で記録 |
| 未実施理由 | なし |

模擬検証の成功はこの表の実測結果を変更しない。
