# Tasks

## 1. 実行基盤

- [x] 1.1 Python の依存関係と CLI エントリポイントを追加し、ヘルプ表示と依存関係のインストールが成功することを確認する。
- [x] 1.2 Codex CLI、`/workspace`、イメージ固有マーカーを含む Docker イメージを作り、ローカル起動でマーカーと `codex exec-server` の存在を確認する。
- [x] 1.3 API キー、制限付き環境キー、モデルの設定読み込みと入力検証を実装し、欠落時の明示的なエラーとキーがイメージ・ログに入らないことを確認する。
- [x] 1.4 ローカルのセッションメタデータ、ログ、秘密ファイルを gitignore 対象にし、サンプル生成後の `git status` で追跡対象にならないことを確認する。

## 2. セッションとコンテナの管理

- [x] 2.1 `self_hosted` セッションを作成・取得し、セッション ID、環境 ID、`remote_url` をホストの非公開メタデータに保存・再読込できるようにし、API 模擬テストで作成と再取得の一致を確認する。
- [x] 2.2 セッション単位の排他制御、Docker ラベル照合、executor コンテナの起動・接続待機を実装し、重複要求が同じ稼働中コンテナに収束するテストで確認する。
- [x] 2.3 ターン結果と保存済み items の取得、ストリーム切断後の結果回復、接続失敗とツール失敗の判定を実装し、各失敗形と成功形の模擬テストで確認する。
- [x] 2.4 追加入力と競合しない停止・再起動、異常終了後の所有コンテナ照合を実装し、停止中の入力とコントローラ再起動のテストで未処理入力・重複コンテナがないことを確認する。
- [x] 2.5 起動・接続・切断・停止・起動失敗の JSON Lines 記録を実装し、イベントごとの ID 対応、UTC 時刻、終了結果、秘密値の不在をテストで確認する。

## 3. 検証シナリオと手順

- [x] 3.1 コンテナマーカーをツールで読み、回答とツール結果を照合するシナリオを実装し、模擬成功・不一致・ツール失敗の判定を確認する。
- [x] 3.2 二つのセッションを並行実行して異なるコンテナに割り当てるシナリオを実装し、セッション・環境・コンテナ ID の対応とマーカー値の分離を確認する。
- [x] 3.3 同一セッションのコンテナ停止後に CLI を再起動して追加入力を送り、会話の合言葉、別セッションの文脈分離、ファイルの非継続をそれぞれ判定するシナリオを実装し、結果レポートで三つの判定が独立して見えることを確認する。
- [x] 3.4 README に環境キー発行と API 権限、Docker・ネットワーク要件、実行コマンド、各合否基準、ライフサイクルログの読み方、実測と未実施の記録欄を記載し、手順が一から再現できることをレビューする。
- [x] 3.5 利用可能な資格情報で実 API の通し検証を行い、マーカー、並行セッション、停止・再開、ID 対応を証拠とともに記録する。利用できない場合は未実施理由を記録し、実測成功と表記しない。
